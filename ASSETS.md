# Asset licenses

DengTa Home source code and project-created interface artwork are released under the root [ISC License](LICENSE), except where a separate license is included next to an asset.

## Project artwork

The app icon, Moments banner and six bear sticker illustrations in `frontend/public/` were created for DengTa Home and are included with this release under the ISC License. The public release uses a code-rendered iridescent `D` mark; third-party product logos and reference-video crops are not included.

## Fonts

Bundled fonts in `frontend/public/fonts/` retain their individual SIL Open Font License 1.1 terms. The corresponding license text for every distributed family is stored in the same directory. See [the font inventory](frontend/public/fonts/README.md).

## Third-party packages

JavaScript dependencies retain their upstream licenses. Exact versions are recorded in `frontend/package-lock.json` and `backend/package-lock.json`.
